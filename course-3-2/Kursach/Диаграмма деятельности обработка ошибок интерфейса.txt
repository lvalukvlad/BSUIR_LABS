@startuml
title Диаграмма деятельности: Обработка ошибок интерфейса

start
:Пользователь задает вопрос в AskAI;
if (Состояние вопроса новое?) then (да)
  :Отправить вопрос один раз;
else (нет)
  :Игнорировать дубликат;
  stop
endif

if (Карточка загружается?) then (да)
  :Показать спиннер;
  if (Загрузка завершилась?) then (да)
    :Отобразить данные;
  else (нет)
    :Повторить через таймаут;
  endif
else (нет)
  :Загрузить данные карточки;
endif

if (Нажата кнопка "Установить"?) then (да)
  :Выполнить установку с await;
  if (Установка успешна?) then (да)
    :Показать успех;
  else (нет)
    :Ошибка установки;
    :Показать ошибку;
  endif
else (нет)
  :Обработать другие события;
endif

stop
@enduml
