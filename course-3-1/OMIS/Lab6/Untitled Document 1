да, переводит, но не на разные языки; с русского на английский ничего не переводит; переводчик не может загружать тексты. 
    ID перевода, ID изменения, ID качества, ID администратора — всё это "внутри" JSON, а не как отдельные сущности (в БД)
    Нет:

    ID словаря, ID корпуса, ID модели — не выделено отдельно	
Нет:

    Загрузка файла (только ввод вручную)
    
        Нет:
        Загрузка файла
        Анализ культурных особенностей (только стиль/домен/аудитория)
        Буквальный/адаптированный/креативный — у тебя стиль: formal/casual/poetic

то не соответствует:

    Загрузка файлов (только ввод вручную)
    Язык определяется вручную (а не автоматически)
    Культурные особенности — упрощённо
    Варианты перевода — не буквальный/адапт./креативный, а стили
    ID сущностей — не выделены как в ER
    
    она не определяет язык
    она не редактирует перевод
    оно не отображает черновой перевод и не формирует его
    не делает исправленной версии ччерновика
    не анализирует культурные особенности
    нет отдельного словаря
    нет интерфейса для комбинации вариантов
    нет роли эксперт, нет консультаций	
    нет state machine
    нет явного описания состояний(Вход, Просмотр, Редактирование, Утверждение, Локализация как состояния UI)
    нет красивых страничек как в макете
    
    Как думаешь, такое уже можно залить на гитхаб. Или же, может, сможешь доделать до того состояния, чтоб можно было залить на гитхаб?
