# Отчёт по практическому занятию №1 (ЯПИС)

Вариант 8. Регулярное выражение: $a^{2}(ab)^{*}(c\mid d)^{+}$.

Тема: **Построение конечного автомата**.

## Сборка

```bash
cd pz1/yapis-report-1
./scripts/render-diagrams.sh
latexmk -pdf course_report.tex
```

Диаграммы: `plantuml/*.puml` → `png/`.
